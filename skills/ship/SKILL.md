---
name: Ship
model: haiku
description: Git add, commit, and push in one command. USE WHEN ship, commit and push, git ship, push it, send it, commit push, acp. NOT FOR syncing a private dotfiles/config repo with its own safety guards — use a dedicated sync skill for that.
---

# Ship

One-command git add + commit + push. Drafts a commit message from the diff, stages files, commits, and pushes.

## Workflow Routing

| Trigger | Workflow |
|---------|----------|
| default | `Workflows/Ship.md` |
